# Simple Calculator

This repository contains a basic command-line calculator written in Python.

## Usage

Run the calculator with:

```bash
python3 calculator.py
```

Then enter expressions in the form `number operator number`.
Type `quit` or `exit` to close the program.
